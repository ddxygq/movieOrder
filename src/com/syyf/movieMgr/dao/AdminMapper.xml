<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.syyf.movieMgr.dao.AdminMapper">
	<select id="querySingle" resultType="admin">
		select * from admin where admin_name=#{admin_name,jdbcType=VARCHAR} and admin_pwd=#{admin_pwd,jdbcType=VARCHAR}
	</select>
</mapper>